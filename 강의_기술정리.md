# learn_HTML 폴더 정리

## 폴더 목적

이 폴더는 HTML의 기본 문서 구조, 태그, 속성, 인라인 스타일, 주석을 연습하는 실습 공간이다. 현재 핵심 파일은 `index.html`이다.

## index.html 구성

현재 HTML 파일에는 다음 내용이 포함되어 있다.

- `<!doctype html>`로 HTML 문서임을 선언
- `<html>`, `<head>`, `<body>` 기본 구조 작성
- `<meta charset="UTF-8">`로 한글 표시를 위한 문자 인코딩 설정
- `<title>`로 브라우저 탭 제목 설정
- `<p>`, `<div>`, `<h1>` ~ `<h6>`, `<strong>`, `<span>` 태그 실습
- `style` 속성으로 배경색과 안쪽 여백 적용
- `<!-- 주석 -->`으로 코드 설명 작성
- `lorem` 더미 텍스트 실습

## HTML 기본 구조

```html
<!doctype html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>문서 제목</title>
  </head>
  <body>
    화면에 보이는 내용
  </body>
</html>
```

| 요소 | 역할 |
|---|---|
| `doctype` | 브라우저에 HTML 문서임을 알려준다. |
| `html` | 전체 문서를 감싼다. |
| `head` | 문서 정보와 설정을 담는다. |
| `body` | 실제 화면에 보이는 내용을 담는다. |
| `meta charset` | 문자 인코딩을 지정한다. |
| `title` | 브라우저 탭 제목을 지정한다. |

## 실습한 태그

| 태그 | 기능 | 현재 실습 예시 |
|---|---|---|
| `p` | 문단을 만든다. | 인사말, 긴 문장 |
| `div` | 여러 요소를 묶는 영역을 만든다. | 분홍색 박스 영역 |
| `h1` ~ `h6` | 제목 계층을 만든다. | 제목 크기 비교 |
| `strong` | 중요한 텍스트를 강조한다. | 굵은 글씨 강조 |
| `span` | 문장 일부를 따로 감싼다. | 빨간색 더미 텍스트 |

## 속성과 스타일

HTML 태그에는 속성을 추가해 더 구체적인 동작이나 스타일을 줄 수 있다.

```html
<div style="background-color: pink; padding: 20px">
  박스 내용
</div>
```

위 코드에서 `style` 속성은 CSS를 태그에 직접 적용한다.

| CSS 속성 | 의미 |
|---|---|
| `background-color` | 배경색 |
| `padding` | 요소 안쪽 여백 |
| `color` | 글자색 |

처음 실습에서는 인라인 스타일을 사용해도 괜찮지만, 프로젝트가 커지면 CSS 파일을 분리하는 것이 좋다.

## 주석

HTML 주석은 코드 설명을 남길 때 사용한다.

```html
<!-- 제목 관련 -->
<h1>h1 제목입니다.</h1>
```

주석은 브라우저 화면에는 보이지 않지만 소스 코드에는 남는다. 따라서 API 키나 비밀번호 같은 민감한 정보는 주석에도 적지 않아야 한다.

## 현재 코드에서 고치면 좋은 점

- `<html lang="en">`을 한글 문서에 맞게 `<html lang="ko">`로 바꾸면 좋다.
- 마지막의 `lorem` 텍스트는 실습 흔적이므로 실제 페이지에서는 문단 태그 안에 넣거나 제거한다.
- 인라인 스타일이 늘어나면 `style.css` 파일로 분리한다.
- AI 챗봇 실습으로 넘어가면 입력창과 버튼을 추가한다.

## 다음 실습 방향

1. `input`과 `button` 태그로 메시지 입력 UI 만들기
2. CSS 파일을 분리해 박스와 버튼 스타일 정리하기
3. JavaScript로 버튼 클릭 이벤트 연결하기
4. 입력한 메시지를 화면에 추가하기
5. 서버 API와 연결해 AI 응답 받기

## 복습 체크리스트

- [ ] HTML 기본 구조를 직접 작성할 수 있다.
- [ ] `head`와 `body`의 차이를 설명할 수 있다.
- [ ] `div`, `p`, `span`, `strong`의 역할을 구분할 수 있다.
- [ ] `style` 속성으로 간단한 CSS를 적용할 수 있다.
- [ ] HTML 주석을 작성할 수 있다.
- [ ] Live Server로 결과를 확인할 수 있다.
